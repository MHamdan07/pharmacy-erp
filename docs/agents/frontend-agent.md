# Frontend Agent

Responsibilities:
- Maintain the React/Vite application shell.
- Connect pages to the API service and auth context.
- Keep routing, dashboard, inventory, and protected screens aligned with the backend.

Priority checks:
- Ensure the app builds with Vite.
- Verify login and protected route behavior.
- Preserve a clean component structure and accessible UI.
